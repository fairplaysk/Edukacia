%h1 Index

%table
  %tr
    %th Name
    %th

  - @quizzes.each do |quiz|
    %tr
      %td= quiz.name
      %td= link_to 'take quiz', new_submission_path(:quiz_id => quiz.id)

%br

= link_to 'New Quiz', new_kwizzer_quiz_path
= link_to 'Rails admin', rails_admin_dashboard_path
