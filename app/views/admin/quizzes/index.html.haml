%h1 Listing quizzes

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @quizzes.each do |quiz|
    %tr
      %td= quiz.name
      %td= link_to 'Show', admin_quiz_path(quiz)
      %td= link_to 'Edit', edit_admin_quiz_path(quiz)
      %td= link_to 'Destroy', admin_quiz_path(quiz), :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Quiz', new_admin_quiz_path
